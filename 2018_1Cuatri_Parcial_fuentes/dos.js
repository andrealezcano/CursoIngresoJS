function mostrar()
{

    var nombreIngresado;

    var localidadIngresado;

    var mensaje;

    nombreIngresado = elNombre.value;

    localidadIngresado = laLocalidad.value;


  mensaje = nombreIngresado + localidadIngresado;

  mensaje = alert("Usted es" + nombreIngresado + "y vive en la localidad de" + localidadIngresado);
  

}
